import React from "react";
import styled from "styled-components";

const Wrapper = styled.section`
  width: 450px;
  height: 250px;
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.3);
`;

const Microlinkdata = () => (
  <Wrapper>These tires can even climb stairs</Wrapper>
);

export default Microlinkdata;
